<!DOCTYPE html>
<html lang="en">
<head>
  <title>JobTrackr</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>
<body>

<div class="container-fluid">
  <div class="row content">
    <div class="col-sm-3 sidenav">
      <h4>JobTrackr</h4>
      <ul class="nav nav-pills nav-stacked">
        <li class="active"><%= link_to "Home", "/" %></li>
        <li><%= link_to "Companies", companies_path %></li>
        <li><%= link_to "Categories", categories_path %></li>
        <% if @category.nil? %>
          <li><%= link_to "Create a new job for #{@company.name}", new_company_job_path %></li>
        <% end %>

      </ul><br>
      <div class="input-group">
          </button>
        </span>
      </div>
    </div>

    <div class="col-sm-9">
      <% if @category.nil? %>
        <h4><small>ALL JOBS FOR <%= @company.name.upcase %></small></h4>
      <% elsif @company.nil? %>
        <h4><small>ALL JOBS FOR <%= @category.title.upcase %></small></h4>
      <% end %>
      <hr>
      <% if @category.nil? %>
        <% @jobs.each do |job| %>
          <ul><li><%= link_to job.title, job_path(job) %> at <%= job.company.name %> </li></ul>
        <% end %>
        <br>
      <% elsif @company.nil? %>
        <% @jobs.each do |job| %>
          <ul><%= link_to job.title, job_path(job) %> at <%= job.category.title %> </ul>
        <% end %>
      <% end %>
      </div>
    </div>
  </div>
</div>

<footer class="container-fluid">
  <p>© 2018 Cameron Johnson</p>
</footer>

</body>
</html>
